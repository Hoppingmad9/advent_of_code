const testing = true;

let data;
if (testing) {
  data = require('./test_input.json')['data'];
} else {
  data = require('./input.json')['data'];
}

/**
 * calcs part a
 * @param {list} data list of inputs
 */
function partA(data) {
}

/**
 * calcs part b
 * @param {list} data list of inputs
 */
function partB(data) {
}

partA(data);
partB(data);
